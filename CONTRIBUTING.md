# Contributing to Telegram Reminder Bot

Спасибо за ваш интерес к проекту! Мы рады любому вкладу.

## Как внести вклад

### Сообщение об ошибках

Если вы нашли ошибку:

1. Проверьте, не была ли она уже сообщена в [Issues](https://github.com/your-repo/issues)
2. Создайте новый Issue с детальным описанием:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Версия Python и зависимостей
   - Логи ошибок

### Предложение новых функций

1. Откройте Issue с тегом "enhancement"
2. Опишите предлагаемую функцию
3. Объясните, зачем она нужна
4. Дождитесь обсуждения перед началом разработки

### Pull Requests

1. **Fork репозиторий**
   ```bash
   git clone https://github.com/your-username/reminder_bot.git
   cd reminder_bot
   ```

2. **Создайте ветку для вашей функции**
   ```bash
   git checkout -b feature/amazing-feature
   ```

3. **Настройте окружение**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   venv\Scripts\activate     # Windows
   pip install -r requirements.txt
   ```

4. **Внесите изменения**
   - Следуйте стилю кода проекта
   - Добавьте docstrings для новых функций
   - Обновите документацию при необходимости

5. **Протестируйте изменения**
   ```bash
   python main.py
   ```

6. **Commit изменения**
   ```bash
   git add .
   git commit -m "feat: добавлена новая функция"
   ```

   Используйте [Conventional Commits](https://www.conventionalcommits.org/):
   - `feat:` - новая функция
   - `fix:` - исправление ошибки
   - `docs:` - изменения в документации
   - `style:` - форматирование кода
   - `refactor:` - рефакторинг
   - `test:` - добавление тестов
   - `chore:` - обновление зависимостей, конфигурации

7. **Push в ваш fork**
   ```bash
   git push origin feature/amazing-feature
   ```

8. **Создайте Pull Request**
   - Опишите ваши изменения
   - Ссылайтесь на связанные Issues
   - Добавьте скриншоты если применимо

## Стиль кода

### Python

- Следуйте [PEP 8](https://pep8.org/)
- Используйте type hints где возможно
- Максимальная длина строки: 100 символов
- Используйте docstrings для функций и классов

Пример:
```python
def calculate_next_datetime(
    current_datetime: datetime.datetime,
    frequency: str
) -> datetime.datetime:
    """
    Calculate next datetime based on frequency.

    Args:
        current_datetime: Current datetime
        frequency: Frequency string

    Returns:
        Next datetime
    """
    # implementation
```

### Структура коммитов

```
<type>(<scope>): <subject>

<body>

<footer>
```

Пример:
```
feat(handlers): добавлен обработчик редактирования напоминаний

Позволяет пользователям редактировать существующие напоминания
через команду /edit<id>

Closes #42
```

## Архитектура проекта

### Структура модулей

- `bot/config.py` - Конфигурация
- `bot/database/` - Работа с БД
- `bot/handlers/` - Обработчики команд
- `bot/keyboards/` - Клавиатуры
- `bot/services/` - Бизнес-логика
- `bot/utils/` - Утилиты

### Добавление нового обработчика

1. Создайте файл в `bot/handlers/`
2. Создайте Router
3. Добавьте обработчики
4. Зарегистрируйте в `main.py`

Пример:
```python
# bot/handlers/my_feature.py
from aiogram import Router

router = Router()

@router.message(...)
async def my_handler(...):
    pass
```

```python
# main.py
from bot.handlers import my_feature_router
dp.include_router(my_feature_router)
```

## Тестирование

Перед отправкой PR:

1. Протестируйте все измененные функции
2. Убедитесь, что бот запускается без ошибок
3. Проверьте, что существующий функционал не сломан

## Вопросы?

Если у вас есть вопросы, создайте Issue с тегом "question" или напишите maintainer'у.

## Code of Conduct

Мы ожидаем от всех участников:

- Уважительного общения
- Конструктивной критики
- Помощи новичкам
- Профессионализма

---

Спасибо за ваш вклад! 🚀
