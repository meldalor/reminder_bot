# Telegram Reminder Bot

Профессиональный бот-напоминалка для Telegram с поддержкой часовых поясов, повторяющихся напоминаний и гибкой настройкой.

## 📋 Возможности

- ⏰ Создание одноразовых и повторяющихся напоминаний
- 🌍 Поддержка различных часовых поясов России
- 📅 Гибкая настройка даты и времени
- 🔄 Настраиваемая частота повторений (минуты, часы, дни, месяцы, годы)
- 📝 Управление напоминаниями (просмотр, удаление)
- 💾 SQLite база данных для надежного хранения

## 🏗️ Структура проекта

```
reminder_bot/
├── bot/
│   ├── __init__.py
│   ├── config.py              # Конфигурация и настройки
│   ├── states.py              # FSM состояния
│   ├── database/
│   │   ├── __init__.py
│   │   └── db.py              # Работа с базой данных
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── start.py           # Обработчик команды /start
│   │   ├── timezone.py        # Настройка часового пояса
│   │   └── reminders.py       # Управление напоминаниями
│   ├── keyboards/
│   │   ├── __init__.py
│   │   └── main_keyboard.py   # Клавиатуры бота
│   ├── services/
│   │   ├── __init__.py
│   │   └── scheduler.py       # Планировщик напоминаний
│   └── utils/
│       ├── __init__.py
│       └── datetime_utils.py  # Утилиты для работы с датой/временем
├── data/                      # База данных (создается автоматически)
├── logs/                      # Логи (создается автоматически)
├── main.py                    # Точка входа приложения
├── requirements.txt           # Зависимости Python
├── .env                       # Конфигурация (не в git)
├── .env.example               # Пример конфигурации
├── .gitignore
└── README.md
```

## 🚀 Установка и запуск

### Предварительные требования

- Python 3.10 или выше
- Токен Telegram бота (получить у [@BotFather](https://t.me/botfather))

### Шаги установки

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd reminder_bot/develope_bot
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
```

3. **Активируйте виртуальное окружение:**

Windows:
```bash
venv\Scripts\activate
```

Linux/MacOS:
```bash
source venv/bin/activate
```

4. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

5. **Настройте конфигурацию:**
```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env и вставьте ваш BOT_TOKEN
```

6. **Запустите бота:**
```bash
python main.py
```

Или используйте `start.bat` на Windows:
```bash
start.bat
```

## ⚙️ Конфигурация

Настройки в файле `.env`:

```env
# Токен Telegram бота
BOT_TOKEN=your_bot_token_here

# Интервал проверки напоминаний (в секундах)
CHECK_INTERVAL_SECONDS=60

# Отступ для повторных напоминаний (в минутах)
REMINDER_OFFSET_MINUTES=15

# Время истечения временных напоминаний (в часах)
TEMP_REMINDER_EXPIRATION_HOURS=1
```

## 📖 Использование

### Основные команды

- `/start` - Запуск бота и настройка часового пояса
- `+` - Создать новое напоминание
- `Мои уведомления` - Просмотр активных напоминаний
- `Изменить часовой пояс` - Изменить часовой пояс
- `/delete<ID>` - Удалить напоминание по ID

### Формат частоты

При создании напоминания можно указать частоту повторения:

- `0` - одноразовое напоминание
- `1min` - каждую минуту
- `1h` - каждый час
- `1d` - каждый день
- `1m` - каждый месяц
- `1y` - каждый год
- `1d 2h 30min` - комбинированная частота

### Формат даты и времени

- Дата: `DD.MM` или `DD.MM.YYYY`
- Время: `HH:MM`
- Можно указать несколько значений через запятую: `15.10,16.10` или `10:00,14:00,18:00`

## 🔧 Разработка

### Архитектура

Проект следует принципам:
- **Модульность**: каждый модуль отвечает за свою область
- **Separation of Concerns**: разделение логики по слоям
- **DRY**: переиспользование кода через утилиты
- **Clean Code**: читаемый и документированный код

### Основные модули

- `config.py` - централизованная конфигурация
- `database/` - слой работы с базой данных
- `handlers/` - обработчики команд и сообщений
- `keyboards/` - клавиатуры и кнопки
- `services/` - бизнес-логика (планировщик)
- `utils/` - вспомогательные функции

### Добавление новых функций

1. Создайте новый обработчик в `bot/handlers/`
2. Зарегистрируйте роутер в `main.py`
3. Добавьте необходимые утилиты в `bot/utils/`
4. Обновите документацию

## 🗄️ База данных

Используется SQLite с двумя таблицами:

**reminders** - хранение напоминаний:
- `id` - уникальный идентификатор
- `user_id` - ID пользователя Telegram
- `name_reminder` - название напоминания
- `frequency` - частота повторения
- `dates` - даты напоминаний
- `times` - время напоминаний
- `active` - статус активности
- `expiration_time` - время истечения (для временных)
- `last_message_id` - ID последнего сообщения

**users** - настройки пользователей:
- `user_id` - ID пользователя Telegram
- `timezone` - часовой пояс пользователя

## 📝 Логирование

Логи записываются в консоль с форматом:
```
YYYY-MM-DD HH:MM:SS - module - LEVEL - message
```

Уровни логирования:
- INFO - информационные сообщения
- WARNING - предупреждения
- ERROR - ошибки

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 🐛 Известные проблемы

Если возникли проблемы, создайте Issue в репозитории.

## 📞 Поддержка

Для вопросов и предложений:
- Создайте Issue на GitHub
- Напишите в Telegram: @kolerts

---
